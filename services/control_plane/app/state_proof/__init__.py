"""Deterministic explanation read model.

Renders grounding (`belief_support` edges) and lineage (the `belief_versions`
supersession chain) as structure. Narration is a presentation concern and does
not live here; `StateProof` is structural, never prose.
"""
