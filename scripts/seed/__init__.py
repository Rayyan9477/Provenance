"""Deterministic seed generation: `ids.py` (the `sid()` helper), `decoys.py`, `embeddings.py`.

Seed dates are stored as offsets from the seed epoch recorded in
`db/seeds/MANIFEST.json`, never as absolute literals.
"""
