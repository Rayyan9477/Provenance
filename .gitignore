# ---------------------------------------------------------------------------
# Secrets. Nothing in this block may ever be committed.
# The repository becomes PUBLIC at submission; a secret committed once is a
# secret leaked permanently, because git history survives deletion.
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
# The `.env*` rules above are anchored to the repository root, so a dotenv in a
# subdirectory is NOT covered by them. deploy/.env.deploy holds two live
# CockroachDB DSNs and the Google API key, which makes this the single most
# important line in this block after the two above it.
**/.env
**/.env.*
!**/.env.example
!deploy/.env.deploy.example
*.pem
*.key
*.crt
!ops/**/*.example.crt
secrets/
*secrets*.json
.aws/
credentials
# The shapes a repository of this kind grows. ops/41_RUNBOOK.md section 2.5 and
# settings.py both say the shell exports the environment, which is precisely the
# direnv use case, so .envrc is the most likely next credential file here.
# ops/gate-env.sh advertises itself as "committed and contains no secret", which
# invites a *.local.sh sibling that is not. (D-00-005.)
.envrc
.netrc
.npmrc
.pypirc
*.local.sh
*.p12
*.pfx
*.jks
id_rsa*
*.tfvars

# Probe transcripts are committed ON PURPOSE (they are gate evidence), but only
# after tools/scrub.py has masked credentials, ARNs and account ids. A raw,
# unscrubbed transcript must never land.
ops/**/*.raw.txt

# T0.2 names this entry and 22_EVAL_DATASETS.md section 1.4 says reports/ is
# "git-ignored except the pinned baseline". Without it, every generated eval
# report would be committed. (D-00-005.)
evals/reports/*.json
!evals/reports/baseline.json
ops/**/*.unscrubbed.txt

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.venv/
venv/
ENV/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.import_linter_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover

# ---------------------------------------------------------------------------
# Node / Next.js
# ---------------------------------------------------------------------------
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ---------------------------------------------------------------------------
# AWS CDK
# ---------------------------------------------------------------------------
cdk.out/
cdk.context.json

# ---------------------------------------------------------------------------
# Editors and OS
# ---------------------------------------------------------------------------
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# ---------------------------------------------------------------------------
# Local scratch
# ---------------------------------------------------------------------------
*.local
tmp/
.scratch/

# The filesystem object store's root under PV_PLATFORM=local
# (storage.objects.DEFAULT_LOCAL_ROOT). These are real artifact bytes written
# by ingestion runs -- the same bytes source_artifacts.s3_key addresses. They
# are reproducible from demo/artifacts/ and belong to a machine, not to the
# repository.
#
# There was no rule for this until 2026-08-28, and `git add -A` would have
# swept them in. A `git check-ignore -v` reading was misread as a match when
# the pattern field it printed was empty; `git add -An` is the check that
# actually answers the question, because it reports what would be staged
# rather than which rule was consulted.
.pv-objects/

# gitleaks JSON reports. A report written INSIDE the scanned tree is itself
# scanned on the next run and the secrets it quotes become fresh findings —
# tools/tests/test_gitleaks_config.py writes its reports outside the tree for
# exactly this reason. One escaped to the repository root during the G0.3
# investigation and was caught only by reading `git add -A` output.
*-report.json
gitleaks-report.json

# Playwright MCP scratch output (console logs, page snapshots, screenshots).
# Written into the working tree by browser verification runs; never a deliverable.
.playwright-mcp/
