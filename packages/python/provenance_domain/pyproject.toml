# packages/python/provenance_domain/pyproject.toml
# Reproduced from specs/11_CONTRACTS.md section 19. Do not add a dependency to
# this manifest without amending that section first: `dependencies = []` is a
# contract, not an oversight.

[project]
name = "provenance-domain"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/provenance_domain"]
