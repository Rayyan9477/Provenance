"""Provenance — the evaluation harness.

`docs/quality/22_EVAL_DATASETS.md` is the specification this tree grows into.
What is built here is the subset that can be **measured** against the world as
it exists on this machine today; every capability that cannot be measured is
reported as `CANNOT RUN` with the thing it waits on, and never as a number.
"""
