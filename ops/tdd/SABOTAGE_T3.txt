T3 SABOTAGE transcript â€” G3.6
Captured 2026-08-18T12:32:44Z

quality/23_PHASE_GATES.md section 9:
  PV_SABOTAGE=provenance_db.retry.is_retryable pytest tests/db/test_retry.py -q; echo "exit=$?"
  -> FAILED test_injected_40001_retries_and_commits; exit=1

The hook replaces provenance_db.retry.is_retryable on the MODULE OBJECT with a
predicate that returns False for everything, which is what G3.6 asks for in so
many words: "remove the retry". The default neutering in
tests/sabotage_matrix.yaml is an identity function; applied to a predicate it
returns the truthy SQLSTATE string, so the loop would keep retrying and G3.2
would still pass - a sabotage that changes nothing. The deviation is recorded at
the foot of provenance_db/retry.py.

test_the_retry_loop_reaches_is_retryable_through_the_module_global asserts,
against the AST, that the loop calls the symbol through its module rather than
through a name captured by a from-import. That is what makes this run
trustworthy: without it the sabotage could silently fail to arrive and a green
run would prove nothing.

$ PV_SABOTAGE=provenance_db.retry.is_retryable python -m pytest packages/python/provenance_db/tests/db/test_retry.py -q
FF.F                                                                     [100%]
================================== FAILURES ===================================
___________________ test_injected_40001_retries_and_commits ___________________

test_db_dsn = SecretStr('**********')
retry_table = '_pv_t3_retry_injected_40001_retries_and_commits'

    async def test_injected_40001_retries_and_commits(test_db_dsn: SecretStr, retry_table: str) -> None:
        """``G3.2``. Two overlapping transactions, twice, then a clean commit.
    
        The final value proves which writes survived: the competitor's two ``+1``
        commits did, both rolled-back ``+10`` attempts did not, and the third
        attempt's ``+10`` did. ``2 + 10 = 12``. A loop that replayed a stale plan
        instead of re-reading would land on ``10``.
        """
        async with await psycopg.AsyncConnection.connect(
            test_db_dsn.get_secret_value(), autocommit=True
        ) as competitor:
            callback, attempts = contending_callback(retry_table, competitor, lambda n: n <= 2)
            async with RolePool(SqlRole.MIGRATOR, ci_source(test_db_dsn), **SMALL) as pool:
>               outcome = await run_in_serializable_tx(pool, callback, config=POLICY)

packages\python\provenance_db\tests\db\test_retry.py:134: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
packages\python\provenance_db\src\provenance_db\retry.py:462: in run_in_serializable_tx
    value = await callback(conn, tx_now)
packages\python\provenance_db\tests\db\test_retry.py:115: in callback
    await conn.execute(f"UPDATE {table} SET v = v + 10 WHERE k = 1")
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

self = <psycopg.AsyncConnection [BAD] at 0x2295040ed50>
query = 'UPDATE _pv_t3_retry_injected_40001_retries_and_commits SET v = v + 10 WHERE k = 1'
params = None

    async def execute(
        self,
        query: Query,
        params: Params | None = None,
        *,
        prepare: bool | None = None,
        binary: bool = False,
    ) -> AsyncCursor[Row]:
        """Execute a query and return a cursor to read its results."""
        try:
            cur = self.cursor()
            if binary:
                cur.format = BINARY
    
            if isinstance(query, Template):
                if params is not None:
                    raise TypeError(
                        "'execute()' with string template query"
                        " doesn't support parameters"
                    )
                return await cur.execute(query, prepare=prepare)
            else:
                return await cur.execute(query, params, prepare=prepare)
    
        except e._NO_TRACEBACK as ex:
>           raise ex.with_traceback(None)
E           psycopg.errors.SerializationFailure: restart transaction: TransactionRetryWithProtoRefreshError: WriteTooOldError: write for key /Tenant/32190/Table/317/1/1/0 at timestamp 1787056534.525646210,0 too old; must write at or above 1787056535.204509593,1: "sql txn" meta={id=f7316c38 key=/Tenant/32190/Table/317/1/1/0 iso=Serializable pri=0.02030609 epo=0 ts=1787056535.204509593,1 min=1787056534.525646210,0 seq=0} lock=true stat=PENDING rts=1787056534.525646210,0 gul=1787056534.775646210,0 obs={n97@1787056534.972987898,0}
E           HINT:  See: https://www.cockroachlabs.com/docs/v26.2/transaction-retry-error-reference.html

C:\Users\rayyan.a\AppData\Local\anaconda3\Lib\site-packages\psycopg\connection_async.py:327: SerializationFailure
________________________ test_retry_exhaustion_raises _________________________

test_db_dsn = SecretStr('**********')
retry_table = '_pv_t3_retry_retry_exhaustion_raises'

    async def test_retry_exhaustion_raises(test_db_dsn: SecretStr, retry_table: str) -> None:
        """``G3.3``. Contention on every attempt exhausts the budget and raises.
    
        Nothing is enqueued and nothing is written: the caller re-drives over
        ``503`` + ``Retry-After``, which is the whole of the recovery path
        (``CANONICAL_DECISIONS.md`` -> *Kernel retry exhaustion*).
        """
        async with await psycopg.AsyncConnection.connect(
            test_db_dsn.get_secret_value(), autocommit=True
        ) as competitor:
            callback, attempts = contending_callback(retry_table, competitor, lambda n: True)
            async with RolePool(SqlRole.MIGRATOR, ci_source(test_db_dsn), **SMALL) as pool:
                with pytest.raises(RetryExhausted) as caught:
>                   await run_in_serializable_tx(pool, callback, config=POLICY)

packages\python\provenance_db\tests\db\test_retry.py:161: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
packages\python\provenance_db\src\provenance_db\retry.py:462: in run_in_serializable_tx
    value = await callback(conn, tx_now)
packages\python\provenance_db\tests\db\test_retry.py:115: in callback
    await conn.execute(f"UPDATE {table} SET v = v + 10 WHERE k = 1")
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

self = <psycopg.AsyncConnection [BAD] at 0x229504197f0>
query = 'UPDATE _pv_t3_retry_retry_exhaustion_raises SET v = v + 10 WHERE k = 1'
params = None

    async def execute(
        self,
        query: Query,
        params: Params | None = None,
        *,
        prepare: bool | None = None,
        binary: bool = False,
    ) -> AsyncCursor[Row]:
        """Execute a query and return a cursor to read its results."""
        try:
            cur = self.cursor()
            if binary:
                cur.format = BINARY
    
            if isinstance(query, Template):
                if params is not None:
                    raise TypeError(
                        "'execute()' with string template query"
                        " doesn't support parameters"
                    )
                return await cur.execute(query, prepare=prepare)
            else:
                return await cur.execute(query, params, prepare=prepare)
    
        except e._NO_TRACEBACK as ex:
>           raise ex.with_traceback(None)
E           psycopg.errors.SerializationFailure: restart transaction: TransactionRetryWithProtoRefreshError: WriteTooOldError: write for key /Tenant/32190/Table/318/1/1/0 at timestamp 1787056545.419747371,0 too old; must write at or above 1787056546.098268833,1: "sql txn" meta={id=ef7c95c6 key=/Tenant/32190/Table/318/1/1/0 iso=Serializable pri=0.01828102 epo=0 ts=1787056546.098268833,1 min=1787056545.419747371,0 seq=0} lock=true stat=PENDING rts=1787056545.419747371,0 gul=1787056545.669747371,0 obs={n97@1787056545.871211333,0}
E           HINT:  See: https://www.cockroachlabs.com/docs/v26.2/transaction-retry-error-reference.html

C:\Users\rayyan.a\AppData\Local\anaconda3\Lib\site-packages\psycopg\connection_async.py:327: SerializationFailure
____ test_the_sqlstate_cockroach_returns_is_the_one_the_classifier_retries ____

test_db_dsn = SecretStr('**********')
retry_table = '_pv_t3_retry_the_sqlstate_cockroach_returns_is_the_one_the_classifier_retries'

    async def test_the_sqlstate_cockroach_returns_is_the_one_the_classifier_retries(
        test_db_dsn: SecretStr, retry_table: str
    ) -> None:
        """The join between the unit classifier and the cluster.
    
        ``test_retry_semantics.py`` asserts that ``40001`` is retryable against a
        constant. This asserts that the constant is the value CockroachDB actually
        sends for the interleaving above — without that, the unit test is a
        tautology about a string this repository wrote down for itself.
        """
        async with (
            await psycopg.AsyncConnection.connect(test_db_dsn.get_secret_value()) as under_test,
            await psycopg.AsyncConnection.connect(
                test_db_dsn.get_secret_value(), autocommit=True
            ) as competitor,
        ):
            with pytest.raises(pgerr.Error) as caught:
                async with under_test.transaction():
                    cur = await under_test.execute(f"SELECT v FROM {retry_table} WHERE k = 1")
                    assert await cur.fetchone() is not None
                    await competitor.execute(f"UPDATE {retry_table} SET v = v + 1 WHERE k = 1")
                    await under_test.execute(f"UPDATE {retry_table} SET v = v + 10 WHERE k = 1")
    
        assert caught.value.sqlstate == "40001"
        assert isinstance(caught.value, pgerr.SerializationFailure)
        assert caught.value.sqlstate in RETRYABLE_SQLSTATES
>       assert is_retryable(caught.value.sqlstate) is True
E       AssertionError: assert False is True
E        +  where False = is_retryable('40001')
E        +    where '40001' = SerializationFailure('restart transaction: TransactionRetryWithProtoRefreshError: WriteTooOldError: write for key /Ten...@1787056570.490352128,0}\nHINT:  See: https://www.cockroachlabs.com/docs/v26.2/transaction-retry-error-reference.html').sqlstate
E        +      where SerializationFailure('restart transaction: TransactionRetryWithProtoRefreshError: WriteTooOldError: write for key /Ten...@1787056570.490352128,0}\nHINT:  See: https://www.cockroachlabs.com/docs/v26.2/transaction-retry-error-reference.html') = <ExceptionInfo SerializationFailure('restart transaction: TransactionRetryWithProtoRefreshError: WriteTooOldError: wri...70.490352128,0}\nHINT:  See: https://www.cockroachlabs.com/docs/v26.2/transaction-retry-error-reference.html') tblen=2>.value

packages\python\provenance_db\tests\db\test_retry.py:229: AssertionError
=========================== short test summary info ===========================
FAILED packages/python/provenance_db/tests/db/test_retry.py::test_injected_40001_retries_and_commits
FAILED packages/python/provenance_db/tests/db/test_retry.py::test_retry_exhaustion_raises
FAILED packages/python/provenance_db/tests/db/test_retry.py::test_the_sqlstate_cockroach_returns_is_the_one_the_classifier_retries
3 failed, 1 passed in 46.70s
exit=1

---- credential hygiene of this very transcript ----
A failing database run is exactly where a credential surfaces: pytest prints
every test-function argument in the failure header. The fixture returns a
pydantic SecretStr for that reason, so the header reads:
  test_db_dsn = SecretStr('**********')
postgres URLs anywhere in the output above: 0
